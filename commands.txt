Macos commands

Move
$ mv [old filepath] [new filepath]

Copy
$ cp -R [old filepath] [new filepath]

.mov -> .mp4
ffmpeg -i my-video.mov -vcodec h264 -acodec mp2 my-video.mp4

Remove Audio (-an flag)
ffmpeg -i $input_file -c copy -an $output_file
